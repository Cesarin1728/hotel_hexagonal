export class Cuarto {
    constructor({ id, nombre, detalles, precio, espacio, imagen_url }) {
        this.id = id; this.nombre = nombre; this.detalles = detalles;
        this.precio = precio; this.espacio = espacio; this.imagen_url = imagen_url;
    }
}